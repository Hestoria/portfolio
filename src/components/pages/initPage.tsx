type Props = {};

const initPage = (props: Props) => {
  return <div
    className="bg-cover bg-center overflow-hidden w-full h-screen"
    style={{ backgroundImage: `url('bg2.jpg')` }}
  >loader</div>;
};

export default initPage;
